# A Minimal Coding Study

We compare two toy encoders under three fixed random seeds. Encoder B improves the mean recovery rate from 0.71 to 0.75, but the sample is too small to establish broad superiority.

## Method

Both encoders use the same preprocessing and decoding budget.

## Result

The observed difference is descriptive; no inferential statistical claim is made.
