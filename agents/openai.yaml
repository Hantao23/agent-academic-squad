interface:
  display_name: "agent学术小分队"
  short_description: "学术任务默认按需触发，也支持显式委派其他任务"
  default_prompt: "Use $agent-academic-squad to route this task to an appropriate planning, execution, or review helper and return the result for my review."

policy:
  allow_implicit_invocation: true
